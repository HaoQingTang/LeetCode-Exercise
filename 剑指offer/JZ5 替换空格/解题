【C++】

一：string加法
class Solution {
public:
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     * 
     * @param s string字符串 
     * @return string字符串
     */
    string replaceSpace(string s) {
        string result="";
        for(auto x : s){
            if(x == ' '){
                result += "%20";
            }
            else{
                result += x;
            }
        }
        return result;
    }
};
